import { LimitLengthDirective } from './limit-length.directive';

describe('LimitLengthDirective', () => {
  it('should create an instance', () => {
    // const directive = new LimitLengthDirective();
    // expect(directive).toBeTruthy();
  });
});
